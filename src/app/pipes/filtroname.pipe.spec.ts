import { FiltronamePipe } from './filtroname.pipe';

describe('FiltronamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltronamePipe();
    expect(pipe).toBeTruthy();
  });
});
